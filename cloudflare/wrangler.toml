name       = "wp-sam"
main       = "worker.js"
compatibility_date = "2024-06-20"

[vars]
# Switch to "live" when ready for production.
STRIPE_MODE = "test"

# ── KV namespace ──────────────────────────────────────────────────────────────
# Create with: wrangler kv namespace create ENTITLEMENTS
# Then paste the returned id below.
[[kv_namespaces]]
binding    = "ENTITLEMENTS"
id         = "03b094a33d484daabb04ad4336e12382"

# ── Routes ────────────────────────────────────────────────────────────────────
# Requires vcns.tech to be an active zone on this Cloudflare account (added as
# a site, nameservers pointed at Cloudflare) before `wrangler deploy` can
# attach this route -- deploy will otherwise fail with a zone-not-found error.
[[routes]]
pattern   = "wp-sam.vcns.tech/*"
zone_name = "vcns.tech"

# ── Secrets (never put values here — use `wrangler secret put` or dashboard) ──
# STRIPE_TEST_SECRET_KEY
# STRIPE_LIVE_SECRET_KEY
# STRIPE_TEST_WEBHOOK_SECRET
# STRIPE_LIVE_WEBHOOK_SECRET
